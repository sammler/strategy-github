
describe.only( 'Sync a profile', () => {

  // All to be moved to moved to other areas, just for testing purposes

  /**
   * Sync the profile, which does the following:
   *
   * - Get the GitHub profile from the authenticated user
   *    - Save the
   *
   */
  it( 'should sync', () => {



  } );

} );
